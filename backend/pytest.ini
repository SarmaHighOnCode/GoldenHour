[pytest]
# Tests import top-level modules (main, store, services...) directly, so run
# from the backend/ directory. `pythonpath = .` makes that work from anywhere.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
